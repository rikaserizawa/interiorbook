<div class="row">
  <div class="col-md-offset-3 col-md-6 col-sm-12">
    <div class="panel panel-danger">
      <div class="panel-heading">
        <%= @user.name %>さんの会員登録削除のページ
      </div>
        <% if current_user == @user %>
          <p>退会すると、インテリア写真の投稿、詳細の閲覧ができなくなってしまいます。</p>
          <%= link_to "退会", @user, method: :delete, data: { confirm: "せっかくの会員登録を本当に削除しますか?" }, class: 'btn btn-danger btn-block' %>
        <% end %>
    </div>
    <div class="text-center"><h4><%= link_to 'MyBookへ戻る', user_path(@user) %></h4></div>
  </div>
</div>
