<div class="topline"></div>
  
  <% if logged_in? %>
  <div class="cover">
    <div class="text-center"><%= link_to(image_tag("/images/logo_b.png") , root_url) %></div>
  </div>
  <% else %>
  <div class="cover">
    <div class="col-md-4 col-sm-6 col-xs-12">
      <div class="text-center"><%= link_to(image_tag("/images/logo_b.png") , root_url) %></div>
    </div>
    <div class="col-md-8 col-sm-6 col-xs-12">
      <div class="cover-inner">
        <div class="cover-contents">
          <div class="text-center">
            <h3>メンバー登録をすると、写真の投稿・詳細閲覧ができます。</h3>
            <%= link_to 'InteriorBookをはじめる', signup_path, class: 'btn btn-danger btn-lg' %>
          </div>
        </div>
      </div>
    </div>
  </div>
  <% end %>
  
<nav class="navbar navbar-expand navbar-default">
    <div class="container">
      <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <% if logged_in? %>
            <p class="navbar-text">ようこそ 、<%= current_user.name %> さん</p>
            <ul class="nav navbar-nav navbar-left">
              <li><%= link_to 'HOME', root_url %></li>
              <li><%= link_to 'MyBook', user_path(current_user) %></li>
              <li><%= link_to 'ログアウト', logout_path, method: :delete %></li>
            </ul>
          <% else %>
            <ul class="nav navbar-nav navbar-left">
              <li><%= link_to 'HOME', root_url %></li>
              <li><%= link_to '新規登録', signup_path %></li>
              <li><%= link_to 'ログイン', login_path %></li>
            </ul>
          <% end %>
        <div class="navbar-form navbar-right" role="search">
          <%= search_form_for @q do |f| %>
            <div class="form-group">
              <%= f.text_field :title_or_appeal_cont, class: "form-control" %>
              <%= f.submit "検索" %>
            </div>
          <% end %>
	  		</div>
			</div>
    </div>
</nav>
